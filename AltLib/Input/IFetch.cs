namespace AltLib
{
    /// <summary>
    /// Parameters for fetching the latest changes from the upstream store
    /// </summary>
    public interface IFetch : ICmdHandler
    {
        // No parameters (for the time being anyway)
    }
}
